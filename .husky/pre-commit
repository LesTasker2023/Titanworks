#!/bin/sh

# Stage all changes automatically
echo "🎯 Auto-staging all changes..."
git add .

yarn lint-staged

# Run enterprise automation after successful lint-staged checks
if [ $? -eq 0 ]; then
  echo "🚀 Running enterprise automation..."
  powershell -ExecutionPolicy Bypass -File scripts/pre-commit-automation.ps1
fi
